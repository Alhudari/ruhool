{
  "installCommand": "npx pnpm@9.15.0 install --no-frozen-lockfile",
  "experimentalServices": {
    "web": {
      "routePrefix": "/",
      "framework": "nextjs",
      "buildCommand": "npx pnpm@9.15.0 turbo run build --filter=@ruhool/web && cp -r apps/web/.next .next && cp apps/web/next.config.js next.config.js"
    }
  }
}
